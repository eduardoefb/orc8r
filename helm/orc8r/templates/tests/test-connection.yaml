apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orc8r.fullname" . }}-test-connection"
  labels:
    {{- include "orc8r.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orc8r.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
