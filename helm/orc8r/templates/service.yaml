apiVersion: v1
kind: Service
metadata:
  name: {{ include "orc8r.fullname" . }}
  labels:
    {{- include "orc8r.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orc8r.selectorLabels" . | nindent 4 }}
