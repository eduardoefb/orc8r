# List of known PLMNS
PLMN: {

{% for p in ueind.plmn_list %}
    PLMN{{ p.id }}: {
           FULLNAME="{{ p.full_name }}";
           SHORTNAME="{{ p.short_name }}";
           MNC="{{ p.mnc }}";
           MCC="{{ p.mcc }}";

    };
{% endfor %}
};

UE0:
{
    USER: {
        IMEI="{{ ueind.user.imei }}";
        MANUFACTURER="{{ ueind.user.manufacturer }}";
        MODEL="{{ ueind.user.model }}";
        PIN="{{ ueind.user.pin }}";
    };

    SIM: {
        MSIN="{{ ueind.sim.msin }}";
        USIM_API_K="{{ ueind.sim.key }}";
        OPC=       "{{ ueind.sim.opc }}";
        MSISDN="{{ ueind.sim.msisdn }}";
    };

    # Home PLMN Selector with Access Technology
    HPLMN= "{{ ueind.hplmn }}";

    # User controlled PLMN Selector with Access Technology
    UCPLMN_LIST = ();

    # Operator PLMN List
    OPLMN_LIST = ("{{ ueind.hplmn }}");

    # Operator controlled PLMN Selector with Access Technology
    OCPLMN_LIST = ("{{ ueind.hplmn }}");

    # Forbidden plmns
    FPLMN_LIST = ();

    # List of Equivalent HPLMNs
#TODO: UE does not connect if set, to be fixed in the UE
#    EHPLMN_LIST= ("20811", "20813");
    EHPLMN_LIST= ();
};